@members1stfederalcreditunion:registry=https://npm.pkg.github.com
always-auth=true

# Do NOT add an authToken here. Authenticate locally with:
#   npm adduser --registry=https://npm.pkg.github.com --scope=@members1stfederalcreditunion
# Or add a user-level ~/.npmrc entry: //npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}